export type Errors = {
  code: string;
  message: string;
};
export type Item = {
  [key: string]: string;
  name: string;
  address: string;
  phone: string;
  collectTime: string;
  frequency: string;
};
